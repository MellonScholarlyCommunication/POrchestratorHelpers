# Etc configuration files

## Orchestrator configuration files

* dev 
* prod

The JSON files in these directories will be evaluated when processin `n3/*` template files
into N3 rules stored in `local/orchestrator`

## Demo.txt

This is a file of extra known data pods, not defined on the local Nifi LDP server. 
